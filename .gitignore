# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Model / torch hub cache
*.pt
*.pth
*.onnx
models/

# OS
.DS_Store
Thumbs.db

# Build
onboard/build/
build/
# any out-of-tree build dir (build-lr, build-vx, build-sb ...)
build*/
*/build*/

# Training output. A 14 MB PPO checkpoint was committed four times by accident
# (nav-sim/runs/ppo_voxel/final.zip); checkpoints and tensorboard logs are
# regenerated by `kestrel train`, not source.
runs/
*/runs/
events.out.tfevents.*

# Recordings: .kdr from `kestrel sim --record` / d435i_probe, .npz captures.
*.kdr
*.npz

# CI staging folder for the Windows zip
nav-sim/dist/
