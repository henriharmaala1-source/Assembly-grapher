<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- USB host is required to talk to the capture dongle over OTG. -->
    <uses-feature android:name="android.hardware.usb.host" android:required="true" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:label="Kestrel Tracker"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <!-- No screenOrientation lock. It used to be "landscape", which forced a
             landscape window onto camera content that needs a 90/270 rotation when
             the phone is held in portrait — the frame could then only ever render
             as a 3:4 strip using ~29% of the screen (measured 668x888 in a
             2263x888 view). Letting the window follow the device makes the window
             shape match the content, so the feed fills it in BOTH orientations.
             configChanges keeps the activity (and the camera session) alive across
             rotation instead of tearing it down and restarting the stream. -->
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Launch / focus the app when the capture dongle is plugged in. -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>
    </application>
</manifest>
