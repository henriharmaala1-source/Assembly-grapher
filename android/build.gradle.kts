// Top-level build file. Plugin versions are pinned here; the app module applies
// them. Bump these to whatever your installed Android Studio / AGP expects — the
// versions below are a known-good 2025-era set, but Android tooling moves fast,
// so treat them as a starting point to reconcile with your SDK, not gospel.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "2.0.20" apply false
}
